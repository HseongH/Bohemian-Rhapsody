// LIBRARY
import React from 'react';
import { Input, Button } from '../elements';

const Header = (props) => {
  return (
  <React.Fragment>
    <Input/>
  </React.Fragment>
  )
};

Header.defaultProps = {};

export default Header;
