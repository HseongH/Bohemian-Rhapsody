// LIBRARY
import React from 'react';
import { Image } from '../elements';

const Post = (props) => {
  return (
    <React.Fragment>
      <Image/>
    </React.Fragment>
  )
};

Post.defaultProps = {};

export default Post;
